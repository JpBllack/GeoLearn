import 'package:flutter/material.dart';

class BandeirasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bandeiras'),
        backgroundColor: Color(0xFF38CFFD),
      ),
      body: Center(
        child: Text('Conteúdo da Tela de Bandeiras'),
      ),
    );
  }
}
